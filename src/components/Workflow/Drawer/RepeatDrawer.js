import {Text, Divider, Button, Container, Image, UnstyledButton, Select, Tooltip, Accordion, Loader, ScrollArea, ActionIcon} from '@mantine/core'
import { useCallback,useEffect, useState } from 'react'
import useStore from '../../../context/store'
import axios from 'axios'
import repeatIcon from '../../../../public/icons/Arrow, Repeat, Rotate.2.svg'


const RepeatDrawer = ({action, toggleMappingModal, sourceNode, targetNode, partnership}) => {

    return (
        <div style={{padding: 12}}>
            <div style={{display: 'flex', flexDirection: 'column'}}>

            </div>
        </div>
    )
}

export default RepeatDrawer