import {Text, Divider, Button, Container, Image, UnstyledButton, Tooltip, Accordion, Loader, ScrollArea, ActionIcon} from '@mantine/core'
import { useCallback,useEffect, useState } from 'react'
import useStore from '../../../context/store'
import axios from 'axios'
import filterIcon from '../../../../public/icons/filter-sort.3.svg'


const FilterDrawer = ({action, toggleMappingModal, sourceNode, targetNode, partnership}) => {

    return (
        <div style={{padding: 12}}>

        </div>
    )
}

export default FilterDrawer